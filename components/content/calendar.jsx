// import CalendarCard from "../table_card";
import { Text } from "@mantine/core";

let Calendar = ({ content }) => {
    return (
        <Text size="md" color="white">
            Calendar
        </Text>
    );
    // return content.map((c) => (

    //     // <div>
    //     //     <TableCard taskName={c.title} label={c.label} dueDate={c.dueDate} />
    //     //     <Space h="md" />
    //     // </div>
    // ));
};
export default Calendar;
